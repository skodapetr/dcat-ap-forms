const reducerName = "dcat-ap.edit";

function reducer(state = {}, action) {
    switch (action["type"]) {
        default:
            return state;
    }
}

export default {
    "name": reducerName,
    "reducer": reducer
};
